namespace RTSEngine.AI.Attack
{
    // Different types of attack priorities -- most resources is the thing that has the most health
    public enum AIAttackTargetPickerType { random, mostAttackResources, leastAttackResources }
}
